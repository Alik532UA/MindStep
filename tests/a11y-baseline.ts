/**
 * База машинно-виявних порушень доступності (ACCESSIBILITY-v8 § 10.1.1,
 * `A11Y-AXE-BASELINE`, HIGH).
 *
 * ## Чому число, а не нуль
 *
 * `expect(results.violations).toEqual([])` виглядає найсуворішим порогом, а
 * насправді найкрихкішим: axe-core додає нові правила в МІНОРНИХ версіях, а
 * `.github/dependabot.yml` щотижня відкриває PR з оновленнями. Тобто гейт із
 * нулем червоніє від оновлення залежності БЕЗ ЖОДНОЇ зміни коду — а червоний
 * гейт, причина якого не в коді, вимикають. Це те саме, що CODE-QUALITY-v8
 * § 6.4.1 каже про `off`: поріг, який доводиться вимикати наступного дня,
 * гірший за відсутній.
 *
 * ## Нуль записується так само, як будь-яке інше число
 *
 * Різниця не в значенні, а в тому, що при зростанні тест падає з переліком, а
 * при зменшенні — просить опустити базу. Обидва повідомлення адресні.
 *
 * ## Перелік `id` окремо від кількості
 *
 * Новий ТИП порушення не ховається за числом: поява невідомого `id` валить
 * прогін навіть у межах ліміту. Саме цей перелік і робить зворотний експеримент
 * можливим — прибраний `aria-label` дає `button-name`, а не «на одиницю
 * більше».
 *
 * ## Версія axe-core
 *
 * Закріплена в `package-lock.json` (`@axe-core/playwright` → `axe-core`).
 * Оновлення, що додало правила, розбирається як робота, а не як «полагодити
 * CI»: нові знахідки або виправляються, або входять сюди з причиною.
 *
 * Заміряно 2026-08-27 на `axe-core@4.13.0`, збірка 0.5.927.
 */

/**
 * Кількість порушень, яку сторінка має право мати. Число лише СПАДАЄ.
 *
 * ## Два наявні борги — обидва `color-contrast`, обидва заміряні
 *
 * | Сторінка       | Елемент                             | Пара              | Ratio | Треба |
 * |----------------|-------------------------------------|-------------------|-------|-------|
 * | `/settings`    | `.styled-button--variant-danger`    | `#fff` на `#f44336` | 3.69:1 | 4.5:1 |
 * | `/local-setup` | `.start-game-btn`                   | `#fff` на `#4caf50` | 2.66:1 | 4.5:1 |
 *
 * «Очистити кеш повністю» — 17.6 px bold, тобто НЕ великий текст (межа 18.66 px
 * для жирного), тож поріг саме 4.5:1, а не 3:1.
 *
 * Джерело в обох випадках одне: білий текст на середньому за світлістю
 * фірмовому кольорі. `--error-color` у `:root` дорівнює `#d32f2f` (5.05:1 —
 * проходить), і саме темні теми перекривають його світлішим `#f44336`
 * (`lib/css/base/variables.css`, рядок 146). `--confirm-action-bg: #4caf50`
 * стоїть у шести файлах тем.
 *
 * ЧОМУ НЕ ПОЛАГОДЖЕНО ТУТ. Це рішення про палітру: шість стилів × дві теми, і
 * будь-яке потемніння міняє вигляд головних кнопок застосунку. Канон
 * (ACCESSIBILITY-v8 § 10.1.1) для цього й тримає базу — щоб борг був названий і
 * вимірний, а не невидимий. Кандидат на виправлення заміряний і лежить поруч:
 * `#d32f2f` дає 5.05:1 із тим самим білим.
 */
export const A11Y_BASELINE = {
	home: 0,
	/** `color-contrast` ×1 — «Очистити кеш повністю», 3.69:1 (див. шапку). */
	settings: 1,
	/** `color-contrast` ×1 — «Почати гру», 2.66:1 (див. шапку). */
	localSetup: 1,
	rules: 0,
	controls: 0,
	rewards: 0,
	join: 0,
	betaChecklists: 0,
	settingsModal: 0
} as const;

/**
 * Відомі `id` правил axe на кожній сторінці — рівність, а не «підмножина».
 *
 * Порожній масив означає «чисто»: будь-який `id`, що зʼявиться, буде названий
 * поіменно, а не розчиниться в лічильнику.
 */
export const A11Y_KNOWN: Record<keyof typeof A11Y_BASELINE, readonly string[]> = {
	home: [],
	settings: ['color-contrast'],
	localSetup: ['color-contrast'],
	rules: [],
	controls: [],
	rewards: [],
	join: [],
	betaChecklists: [],
	settingsModal: []
};
