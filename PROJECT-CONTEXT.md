---
Назва: Контекст проєкту MindStep
Тип: персональний шар інструкцій v9
Дата оновлення: 2026-09-11
---

# Контекст проєкту: MindStep

Цей файл — те, чого немає в пакеті інструкцій v9: специфіка саме цього проєкту.
AI-агент читає його **перед** застосуванням пакета. Усі значення нижче взяті з
коду й конфігів, а не з пам'яті; де сказано «немає» — там справді немає.

## Базові параметри

| Параметр                               | Значення                                                                                                                                     |
| -------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| Профіль (див. README пакета)           | static                                                                                                                                       |
| Adapter                                | `@sveltejs/adapter-static`, `fallback: '404.html'` (SPA-режим)                                                                               |
| Хостинг                                | GitHub Pages                                                                                                                                 |
| Origin                                 | <https://alik532ua.github.io/MindStep/>                                                                                                      |
| base path                              | `/MindStep` у продакшн-збірці, `''` у `dev` (`svelte.config.js`, рядок 4)                                                                    |
| Спільний origin з іншими застосунками? | так — GitHub Pages акаунта                                                                                                                   |
| PROJECT_PREFIX (для сховищ)            | `mindstep_`                                                                                                                                  |
| Префікс dev-середовища                 | не застосовується — dev-прев'ю ділить префікс із продакшном                                                                                  |
| Бекенд                                 | Firebase: Firestore + Realtime Database (`database.rules.json`, `firestore.rules`)                                                           |
| Порти емулятора                        | firestore **8080**, database **9000**, auth **9099** — сусідній `VetCrewGames` навмисно взяв інші (9010/9109), `Slovko` теж (8082/9002/9098) |
| Порти dev-сервера                      | **5399** для агента (`.claude/launch.json`), **5373** для e2e (`playwright.config.ts`), обидва зі `--strictPort`; типовий Vite **5173** не використовується в тестах ніде (`CI-E2E-TARGET-IDENTITY`) |
| Гілки                                  | `main` — продакшн, `dev` — прев'ю (`deploy-dev.yml`)                                                                                         |

## Хмарна база: стан правил доступу

Заповнено за `CLOUD-DATABASE-v8` § 2.3. Правила — **єдина** межа безпеки
онлайн-частини: публічний конфіг Firebase лежить у бандлі, тож адресу бази знає
кожен, хто відкрив сайт.

| Питання                                        | Відповідь                                                                                                                                                                                                                                                                                                                                                                                             |
| ---------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Правила лежать у репозиторії?                  | так — `firestore.rules`, `database.rules.json`                                                                                                                                                                                                                                                                                                                                                        |
| `firebase.json` прив'язує їх?                  | так                                                                                                                                                                                                                                                                                                                                                                                                   |
| Гейт над емулятором                            | `npm run check:rules` — і позитивні, і негативні випадки; числа — рядки `rules-cases` / `rules-cases-denied` у «Числах під гейтом». За v9 (`CDB-RULES-GATE`) одиниця обліку — кожен `allow` з хоча б одним позитивним і одним негативним випадком, а не пропорція; тут це ще **не доведено**: розбір `CASES` по колекціях 2026-09-02 показав, що `rewards` має лише негативні випадки, а `moves`, `votes` і `messages` рахуються разом із `rooms`. Борг, не відхилення                                                                                                                                                                                                                                                                                              |
| Гейт у CI?                                     | **так**, окремий джоб `rules` у `deploy.yml` із `setup-java`                                                                                                                                                                                                                                                                                                                                          |
| Викладання                                     | `firebase deploy --only firestore:rules,database` або через консоль                                                                                                                                                                                                                                                                                                                                   |
| Стан консолі звірений із файлом?               | **НЕ звірений після ревізії 2026-08-18 (друга)** — правила переписані: `update` звужений по полях, вхід гостя дозволений окремим випадком, підколекція присутності прибрана, журнал append-only, межі на відгук і лічильники. У консоль ще не викладені. Попередній зонд (`/rooms`, `/users`, `/rewards` → `403`, `/leaderboards` → `200` навмисно) лишається правдою, але НОВІ звуження там відсутні |
| Індекси у репозиторії?                         | **так, 2026-08-18** — `firestore.indexes.json`, прив'язаний через `firebase.json`. Один складений індекс: `rooms` (`isPrivate` ASC, `lastActivity` DESC) під запит лобі. Доти він існував **лише в консолі**: складений запит без індексу дає готове посилання на створення, тож дефект зникає за десять секунд і назавжди лишається поза репозиторієм (§ 2.4)                                        |
| Індекси викладені в консоль?                   | **не звірено.** Емулятор індексів НЕ перевіряє — він виконує будь-який запит, тож ні гейт правил, ні жоден локальний прогін цього не побачать. Єдина перевірка — інваріант «складений запит має запис у файлі» в `src/cloud-database.spec.ts` (§ 7.4)                                                                                                                                                 |
| Список дозволених доменів звірений?            | не звірений                                                                                                                                                                                                                                                                                                                                                                                           |
| Публічний конфіг постачається (SECURITY § 4.1) | `VITE_`-змінні + `.env.example` — **вибір за правилом**, не відхилення: значення публічне за побудовою, а `$env/static/public` вимагав би секретів у CI, яких для публічного ключа не існує                                                                                                                                                                                                           |

### Що лишилося перевірити людині

Правила викладені й звірені зондом. Чого зонд НЕ доводить — що застосунок під
ними працює. Пройти на двох пристроях:

1. створити кімнату, зайти другим, зробити по ходу кожним;
2. дочекатися «немає ходів» і проголосувати «продовжити» — це найтонший шлях
   нової моделі: господар починає **новий відрізок** журналу, і мають зберегтися
   позиція фігури та рахунок, а лічильники відвідувань — обнулитися;
3. вийти зі сторінки й повернутися — партія має відтворитися перепрогоном.

4. **вхід гостя** — це шлях, який доти правила забороняли: `memberOf(resource)`
   дивиться на СТАРИЙ документ, а той, хто заходить, у складі ще не значиться.
   Тепер для нього є окрема умова `joining()`, і гейт її перевіряє, але живим
   екраном це не пройдено;
5. **кінець партії доїжджає до суперника** — доти він писався в
   `votes/__match`, а правило звіряє ідентифікатор документа з `auth.uid`, тож
   запис відкидався ЗАВЖДИ. Перевірити треба обидва напрямки: коли партію
   завершує господар і коли гість.

6. **Нове звуження 2026-09-02 у консоль ще не викладене.** `rooms/*/messages` і
   `feedback/*/entries` тепер вимагають `createdAt == request.time`, а чат
   заразом отримав перелік ключів і межі довжини. Поки в консолі старі правила,
   застосунок працює як раніше (старі правила ширші), але й підробити позначку
   часу далі можна. Клієнт уже пише `serverTimestamp()`, тож викладання нічого
   не ламає: перевірено гейтом на емуляторі, обидва боки.

**Присутність — перше, що зламається**, якщо рядок `auth != null` у
`database.rules.json` виявиться зарано: симптом — «гравець назавжди офлайн».
Стара причина не вимагати авторизації («`onDisconnect` реєструється до
завершення анонімного входу») більше не діє — `roomService.joinRoom` сам чекає
на вхід, — але перевірено це міркуванням, а не прогоном.

Розмір проєкту — у таблиці «Числа під гейтом» нижче: він найбільший із семи.

## Числа під гейтом

Тут лежать усі числа, якими цей документ описує **поточний** стан репозиторію.
Розсипані по тексту вони застарівали мовчки: § 5.5 пакета вимагала їх виміряти,
і кожне з них колись справді заміряли — а далі читач мав повне право вірити
числу, яке розійшлося з дійсністю через кілька комітів
(AI-AGENT-PITFALLS-v8 § 5.5.1, `PIT-NUMBER-UNDER-GATE`).

Кожен рядок звіряється з дійсністю в `src/doc-numbers.test.ts`
(`GATE-DOC-NUMBERS`) — і **в обидва боки**: ключ, прибраний із таблиці, валить
прогін так само, як розбіжність числа. Правити тут, а не в прозі; правити тим
самим комітом, який змінив дійсність.

| Ключ                 | Число | Що це і чим міряється                                                          |
| -------------------- | ----- | ------------------------------------------------------------------------------ |
| `svelte-files`       | 183   | компонентів і сторінок: `find src -name '*.svelte' \| wc -l`                    |
| `ts-files`           | 375   | модулів TypeScript: `find src -name '*.ts' \| wc -l`                            |
| `routes`             | 21    | маршрутів: `find src/routes -name '+page.svelte' \| wc -l`                      |
| `unit-spec-files`    | 53    | файлів юніт-перевірок — маска `test.include` у `vite.config.ts`                 |
| `e2e-spec-files`     | 19    | файлів e2e: `find tests/e2e -name '*.spec.ts' \| wc -l`                         |
| `e2e-in-ci`          | 7     | з них названі кроком `npx playwright test` у `.github/workflows/`               |
| `rules-cases`        | 89    | випадків у гейті правил бази — масив `CASES` у `scripts/check-rules.mjs`        |
| `rules-cases-denied` | 59    | з них негативних (`allowed: false`) — «сторонній не мусить цього могти»         |
| `contrast-debt`      | 33    | пар палітри нижче порогу контрасту — `KNOWN_CONTRAST_DEBT`                      |

**Що показав перший прогін цього гейта.** Вісім рядків із дев'яти розійшлися, і
найгірше — не на одиниці: гейт правил бази виріс із 54 випадків до **83**, а
борг палітри, навпаки, СКОРОТИВСЯ з 46 до 41 — тобто документ приховував не лише
недоробку, а й зроблену роботу.

**Чого тут немає навмисно.** Боргу ESLint: він уже стоїть під гейтом у мапі
`DEBT` (`src/eslint-baseline.test.ts`), яка звіряється на РІВНІСТЬ в обидва боки.
Друга копія того самого факту рветься саме тому, що вона друга — § 5.5.1 називає
це прямо, тож у прозі борг тепер згадується посиланням на мапу, а не числом.
Кількості самих перевірок (не файлів) тут теж немає: її називає підсумковий
рядок `npx vitest run`, і статично її не порахувати без розбору `describe`.

## Прийняті рішення

Місця, де пакет дає вибір. Незаписане рішення через півроку не відрізнити від
недогляду, і агент щоразу пропонуватиме «виправити» те, що є рішенням.

| Питання                                                                        | Обрано                                                                                                                             | Причина                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Дата       |
| ------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- |
| Бібліотека i18n (I18N § 1)                                                     | `svelte-i18n` + власні модулі словників по мовах                                                                                   | словники розбиті на файли за фічами (`gameBoard`, `faq`, `controlsPage`…), а не один JSON                                                                                                                                                                                                                                                                                                                                                                                                                                                              | до 2026-08 |
| Мови                                                                           | uk (типова), en, nl, crh                                                                                                           | crh — кримськотатарська, перекладено вручну                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | до 2026-08 |
| Інструмент аналітики (ANALYTICS § 1)                                           | GA4, ID у коді (`G-CPGNM62XZW`)                                                                                                    | measurement ID публічний за призначенням; змінна середовища додала б тихий збій при відсутності                                                                                                                                                                                                                                                                                                                                                                                                                                                        | до 2026-08 |
| Згода на збір даних (ANALYTICS § 4)                                            | без банера                                                                                                                         | аудиторія — не ЄЕЗ; рішення переглянути, якщо з'явиться європейський трафік                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | до 2026-08 |
| Модель версіонування (VERSIONING § 1.2)                                        | авто-бамп patch у pre-commit хуці + `static/version.json`                                                                          | номер не редагується руками ніколи                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | до 2026-08 |
| Середовище юніт-тестів                                                         | jsdom (`vitest`)                                                                                                                   | **вибір за правилом CODE-QUALITY § 4.1**, а не прогалина: браузерний режим потрібен компонентам, що залежать від розмірів, layout чи фокуса; тут перевіряються логіка й сервіси                                                                                                                                                                                                                                                                                                                                                                        | до 2026-08 |
| Пошук мертвого коду                                                            | `knip` (`npm run lint:knip`)                                                                                                       | 500 файлів, ручний огляд не масштабується                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | до 2026-08 |
| PWA                                                                            | `vite-plugin-pwa` + власний `ReloadPrompt`                                                                                         | потрібна офлайн-гра й підказка про оновлення                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | до 2026-08 |
| Стратегія testid у production (TESTID § 1.11)                                  | **B — зберігати**                                                                                                                  | плагіна видалення немає, отже де-факто B; записано, а не вдається за A                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 2026-08-15 |
| Гейти над зібраним виводом (SEO § 6.1, SECURITY § 16)                          | власний `scripts/check-build.mjs`, а не окремий пакет                                                                              | правило «непорожнє тіло» перероблене під SPA-профіль: перевіряється наявність бутстрапа, а не 200 символів тексту                                                                                                                                                                                                                                                                                                                                                                                                                                      | 2026-08-16 |
| Перевірка правил доступу (SECURITY)                                            | `scripts/check-rules.mjs` над емулятором, **джоб `rules` у CI**                                                                    | файл лежить у `scripts/`, а не серед тестів, щоб не бути перевіркою, якої не запускає ніхто. Токени бере з емулятора Auth, тож перевіряє правила так само, як їх бачить клієнтський SDK; `firebase-admin` ходив би в обхід правил                                                                                                                                                                                                                                                                                                                      | 2026-08-18 |
| Модель онлайн-партії (CLOUD-DATABASE § 8)                                      | **журнал ходів**, а не поле стану                                                                                                  | стан партії — чиста функція від (`seed`, склад, журнал), тож у базі лежить лише опис і `moves/{seq}`, кожен із яких можна лише СТВОРИТИ. Доти вся партія була полем `gameState` одного документа, який писали обидва гравці                                                                                                                                                                                                                                                                                                                            | 2026-08-18 |
| Присутність — RTDB, решта — Firestore                                          | дві бази навмисно                                                                                                                  | `onDisconnect` виконує сервер, коли клієнт зник; у Firestore такого механізму немає, і офіційна порада — підключити поруч RTDB                                                                                                                                                                                                                                                                                                                                                                                                                         | 2026-08-18 |
| Присутність — ОДНЕ джерело правди                                              | RTDB `/status/{room}/{uid}`; поле `players.{uid}` у кімнаті — **явно похідна** копія з ЄДИНИМ шляхом запису (монітор господаря)    | доти той самий факт лежав у трьох місцях, і дзеркало `rooms/{id}/presence` було найгіршим: його писав САМ КЛІЄНТ, коли помічав обрив, — тобто в єдиному випадку, для якого присутність існує, писати вже нікому, і документ назавжди лишався «на звʼязку», поки `onDisconnect` у RTDB казав протилежне. Копія в документі кімнати лишається, бо лобі перелічує кімнати запитом до Firestore і не може підписатися на RTDB для кожної; § 5.2 такий випадок дозволяє за умови, що копія названа похідною                                                 | 2026-08-18 |
| Журнал ходів **append-only**: сегмент замість стирання                         | `moves/{segment}-{seq}`, видалення заборонене всім                                                                                 | «нова партія в тій самій кімнаті» через стирання давала три дефекти: читання всього журналу заради видалення, межу батча 500 (партія довша за 499 ходів не перезапускалася) і `get()` на кімнату в правилі `delete` при межі 20 звернень на батч — тобто перезапуск упирався у ПРАВИЛА. Тепер записується одне поле, і атомарність § 8.6 виходить сама                                                                                                                                                                                                 | 2026-08-18 |
| Статичний `import` SDK замість `await import()` (§ 10.2)                       | **форма — відхилення, мета — виконана.** SDK лежить окремим чанком 738 КБ і **не** потрапляє в `modulepreload` початкової сторінки | § 10.2 сам називає справжню перевірку: «чанк із SDK не має бути в `modulepreload` початкової сторінки». Vite ділить збірку по маршрутах, і жоден із тринадцяти файлів, що торкаються SDK, не доїжджає до кореневого layout. Заміряно 2026-08-18: 29 передзавантажених файлів, серед них SDK нема. Перевірка стоїть у `scripts/check-build.mjs` — щойно якийсь сервіс із SDK потрапить у кореневий шар, гейт впаде, і тоді ліниві імпорти стануть обовʼязковими. Асинхронні акцесори зараз означали б `await` у 25 місцях без жодного вимірного виграшу | 2026-08-18 |
| Екран помилки — без словника (I18N § 2)                                        | `+error.svelte` і `ErrorBoundary` лишають текст у розмітці                                                                         | їх видно рівно тоді, коли застосунок уже зламався. Залежність екрана помилки від i18n означала б, що він падає разом із підсистемою, про збій якої мусить розповісти. Обидва названі виключенням прямо в `src/i18n-conventions.spec.ts`, а не мовчать                                                                                                                                                                                                                                                                                                  | 2026-08-19 |
| Панель монітора мережі перекладена, хоч і dev-only                             | чотири мови, як усе інше                                                                                                           | вона несе `aria-label`, тобто підпадає під власний інваріант проєкту; хардкод був би винятком, який довелося б окремо обґрунтовувати. Ціна — гард `$i18nReady` на місці монтування, без якого перший `$t` валить застосунок                                                                                                                                                                                                                                                                                                                            | 2026-08-19 |
| Чеклист бета-тестування — ДАНІ, а не Markdown (BETA-CHECKLIST § 5)             | `src/lib/beta/` + сторінка `/beta-test-checklists/`, 50 пунктів у 8 вкладках, дві мови                                             | попередній лежав `.private/docs/testing/checklist/detailed-checklist-v1.md` сім місяців і встиг набути обох симптомів, які канон називає ціною тексту: посилання на маршрут `/game/vs-computer`, якого не існує, і 44 галочки без версії збірки. Беклог фіч із частин 3–5 того файлу НЕ перенесено — це завдання, а не перевірки                                                                                                                                                                                                                       | 2026-08-19 |
| `noindex` чеклиста перевіряється e2e, а не над `build/` (BETA-CHECKLIST § 5.5) | скрипт над `build/` стежить за `Disallow` у `robots.txt`; тег `<meta name="robots">` перевіряє Playwright                          | у цьому профілі жоден маршрут не має власного HTML: `+layout.ts` вимикає SSR, і всі адреси, крім кореня, віддаються з одного `404.html` (фолбек SPA). Тег зі `<svelte:head>` виставляє клієнт після гідрації, тож у поданому HTML його немає й бути не може. `robots.txt` при цьому діє незалежно від того, чи виконує краулер JS — тобто обіцянку тримає саме він                                                                                                                                                                                                         | 2026-08-19 |
| Деплой-екшен із `contents: write` (CI-CD § 1.1)                                | `peaceiris/actions-gh-pages@v4`, `permissions: contents: write` у `deploy.yml` і `deploy-dev.yml`                                  | **вибір за правилом, а не відхилення.** Канон дозволяє рівно один виняток із OIDC-дефолту — dual deploy, коли прев'ю живе на тому самому Pages-сайті. Тут саме він: `main` викладає в корінь, `dev` — у підтеку `dev/`, і крок «Preserve dev folder» у `deploy.yml` існує, щоб продакшн не зніс прев'ю. `actions/deploy-pages@v4` кладе один артефакт на весь сайт і другого середовища не має. Гейти на PR при цьому винесені в окремий `ci.yml` із `contents: read`: workflow, що виконує код із PR, права запису не отримує                         | 2026-08-20 |
| Гейти на Pull Request — окремий `ci.yml` (CODE-QUALITY § 6.1)                  | `on: pull_request`, ті самі гейти без деплою                                                                                       | доти на PR не виконувалося нічого: `deploy.yml` слухає `push` у `main`, `deploy-dev.yml` — лише `workflow_dispatch`. Dependabot щотижня відкриває до п'яти PR, і всі вони мерджилися без жодної перевірки. Секрети в `ci.yml` навмисно не передаються: у PR від Dependabot і з форків їх немає за побудовою, а збірка їх не потребує — SSR вимкнено, тож під час prerender код застосунку не виконується (перевірено: збірка без `.env` проходить, `check:build` теж)                                                                                  | 2026-08-20 |
| Перелік тем `logService` — тип, виведений із конфігу                           | `keyof typeof initialConfig`, а не окремий список                                                                                  | продубльований перелік розійшовся б із конфігом на першій новій темі, і побачити це можна було б лише як «чомусь не типізується». Ключі — ідентифікатори, не рядки в лапках: назва теми в лапках прочиталася б інваріантом `cloud-database.spec.ts` як звернення до колекції бази                                                                                                                                                                                                                                                                      | 2026-08-19 |
| Бюджет бандла (PERFORMANCE § 1) — три числа, а не одне                        | код критичного шляху, CSS і дані окремо; стелі лежать у `scripts/check-build.mjs`, тут не дублюються | § 1 дає дефолт «initial JS ≤ 150 КБ gzip», але одне сумарне число рахує разом із кодом і ДАНІ, що їдуть у бандл модулем (§ 1.1). Дані тут — словники i18n, кожен важить як половина критичного шляху; сьогодні вони лінивими імпортами й на перший кадр не потрапляють, і гейт це ДОВОДИТЬ, а не припускає. JS і CSS розділені, бо лікуються протилежно: JS — поділом чанків, CSS — прибиранням дубльованих правил. Запас у стелях ~10 %, а не 60 %: бюджет із запасом у півтора раза ловить лише катастрофу | 2026-09-02 |
| Фолбек SPA — `404.html`, а не `index.html` (DEPLOY-ENVIRONMENTS § 3)           | `fallback: "404.html"`; власний редирект-`404.html` зі `static/` ВИДАЛЕНО | GitHub Pages віддає `404.html` за невідомою адресою, НЕ переходячи нікуди, тож клієнтський роутер бачить потрібний шлях — це єдиний спосіб мати глибокі посилання. Доти фолбеком був `index.html`, а дірку затикав ручний шим у власному `404.html` зі `static/`: він клав адресу в `sessionStorage` і перекидав на корінь, але `history.replaceState`, який мав її повернути, стояв У ТОМУ Ж файлі замість `app.html`, тобто на сторінці, яку meta-refresh уже зносив. Половина шима не виконувалася ніколи: `/MindStep/beta-test-checklists` мовчки показував головне меню (заміряно на бойовому сайті 2026-09-14). Два наслідки, кожен вартий окремо: (1) фолбек ПЕРЕЗАПИСУВАВ пререндерений `index.html`, тож головна їхала голим каркасом і підтягувала свої 8 стилів уже після старту JS — тепер вона передрендерена, і `CSS_BUDGET_KB` піднято з 23 до 30, бо гейт нарешті міряє сторінку, а не каркас; (2) `404.html` був винятком `NOT_APP_PAGES` у `scripts/check-build.mjs` — виняток пережив своє обґрунтування й зробив дефект невидимим для ВСІХ перевірок. Тепер перевіряються обидві оболонки, плюс окремий гейт на редирект у `404.html` і на існування `index.html` файлом (інакше головна віддавалася б зі статусом 404) | 2026-09-14 |

### Фон першого кадру: `light-dark()` замість зашитого темного

Фон до приходу `app.css` задається інлайновим `<style>` у `src/app.html`. Доти
там стояв `background-color: #1a1a1a` плюс окреме правило
`html[data-theme='light']`, і працювала ця пара лише поки виконується
інлайн-скрипт: `data-theme` ставить саме він.

Інлайн-скрипт у зібраній версії пускає **хеш** у CSP. 2026-08-23 у сусідньому
`teatralo4ka` рівно такий скрипт був заблокований через розбіжність хеша (CRLF
проти LF), і симптомом стала зникла заставка — не помилка в консолі, а
«просто інший вигляд». Тут тим самим шляхом людина зі світлою системною темою
отримала б ТЕМНИЙ перший кадр.

Тепер: `color-scheme: light dark` і `background-color: light-dark(#f5f6fa, #1a1a1a)`.
Системна перевага — це CSS, і вона діє до виконання будь-якого JS. Скрипт
лишається, але відповідає тільки за ЯВНИЙ вибір зі сховища, і перекриває
системну перевагу перемиканням схеми (`html[data-theme] { color-scheme: ... }`),
а не другим набором кольорів — тож колір фону лишається в одному місці.

Заміряно в браузері: явно світла — `light` / `#f5f6fa`; явно темна — `dark` /
`#1a1a1a`; без вибору — `light dark` і фон за системною темою.

**Що саме перевірено, а не припущено.** Стан «скрипта немає зовсім» заміряно в
браузері з прибраними `data-theme` і `data-style`: тло `#f0f2f5`, текст `#333` —
тобто СВІТЛА палітра з `:root` у `base/variables.css`, і вона читабельна. Тобто
зміна не могла зробити гірше: до неї перший кадр був завжди темний, і людині зі
світлою системною темою він змінювався на світлий; тепер перший кадр і кінцевий
збігаються.

**Борг, і його розмір названий точно.** Переведено одну властивість — фон
першого кадру. Палітра тут двовимірна: вісь `data-theme` має РІВНО ДВА значення
(`light`/`dark`), а вісь `data-style` — шість (`gray`, `blue`, `green`, `orange`,
`purple`, `wood`), і файли тем адресують перетин парами селекторів виду
`[data-style="gray"][data-theme="light"]`. Тобто `light-dark()` тут ЗАСТОСОВНИЙ
(двох значень на осі схеми досить) і згорнув би 12 блоків у 6.

Але виграшу «читається без JS» це не дало б: `data-style` теж ставить скрипт, і
без нього не діє жоден із цих блоків — палітру дає `:root`. Тобто це
упорядкування коду, а не виправлення дефекту, і його ціна — переверстка шести
файлів тем із візуальною перевіркою кожної пари. Записано боргом саме з цією
оцінкою, щоб наступний, хто читатиме, не взявся за нього як за «те саме, що
зробили для фону».

**Гейт на цей борг стоїть ЗАЗДАЛЕГІДЬ (2026-08-27).** Канон v8.11
(`UIUX-LIGHT-DARK-COLOR-ONLY`, HIGH) додав до `GATE-CSS-VARS` два класи, і
другий із них існує рівно через таку відкладену переверстку: механічний прохід по
файлу теми затягує в `light-dark()` НЕКОЛІРНІ токени — вони стоять у тому самому
блоці, виглядають так само й мають таку саму пару значень. Наслідок не «взяти
перший аргумент», а зникнення властивості цілком (`box-shadow: none`,
`backdrop-filter: none`), і симптом вказує не туди. У чотирьох сусідніх проєктах
це вже сталося; тут `src/css-variables.spec.ts` тепер червоніє на першому такому
виклику — до того, як він поїде у прод, а не після.

## Обрані optional-файли пакету

- [x] I18N-v9.md — чотири мови
- [x] ANALYTICS-v9.md — GA4, без банера
- [x] AUTH-FORM (product_criteria/v1) — Firebase Auth, є вхід і онлайн-режим; у v9 переїхав із інженерного пакета в продуктовий
- [x] VERSIONING-v9.md — версія в `static/version.json`; її читає `appInitializationService` для виявлення оновлення
- [x] DEBUGGING-v9.md — `logService` + кнопка копіювання логів
- [x] DEPLOY-ENVIRONMENTS-v9.md — окремий dev-прев'ю (`deploy-dev.yml`)
- [x] DOCUMENTATION-v9.md — ведеться `.private/docs/`
- [ ] SCROLLBAR (product_criteria/v1) — власної смуги немає й не потрібна
- [ ] AI-PROVIDERS-v9.md — проєкт не звертається до LLM
- [ ] OBSERVABILITY-v9.md — зовнішнього трекера помилок немає (див. борги)

## Що не перевіряється автоматично

Пакет вимагає, щоб кожне обов'язкове правило мало спосіб перевірки. Де перевірки
немає — це записано тут, а не мовчить. Список має **скорочуватися**: кожен рядок —
борг, а не властивість проєкту.

| Правило                                                                           | Чому перевірки немає                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | План                                                                                                                                                                                                                                                                              |
| --------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ~~a11y-аудит цілої сторінки (ACCESSIBILITY § 8)~~ | **закрито 2026-08-27.** `tests/e2e/a11y.spec.ts` + `tests/a11y-baseline.ts`: вісім сторінок і одна ВІДКРИТА модалка, теги `wcag2a/2aa/21a/21aa/22aa`, поріг — база, що лише спадає, плюс окремий перелік `id` правил (новий ТИП порушення валить прогін навіть у межах ліміту). Стоїть у `ci.yml` і `deploy.yml`. Перший прогін знайшов `aria-hidden-focus` на семи сторінках із девʼяти — див. нижче | — |
| Палітра: пари нижче порогу контрасту (ACCESSIBILITY § 6) — число в рядку `contrast-debt` | **заміряно, не полагоджено.** `src/contrast.spec.ts` розвʼязує токени для всіх вісімнадцяти комбінацій (шість стилів × три теми) і тримає перелік у `src/contrast-baseline.ts`; рядок прибирається, коли пару полагоджено, і гейт червоніє в обидва боки. Кожен рядок — рішення про палітру: колір тримає впізнаваність стилю, тож механічне потемніння наосліп дало б пару, якої не було ні в одній темі (UI-UX § 1.5.1.1) | починати з `orange` (найдорожчий стиль, причина одна: помаранчевий у ролі тла завжди світлий, тож там потрібен ТЕМНИЙ текст, а не білий) і з `--cell-dark -> --piece-color` — 1.07:1 у green/light, фігуру гри майже не видно на клітинці |
| ~~`Tooltip.svelte` на `export let` (SVELTE-CORE § 6)~~                            | **закрито 2026-08-20.** `bind:offsetWidth`/`bind:offsetHeight` роблять вимір реактивним значенням, яке веде Svelte, а позицію — звичайним `$derived`; `$effect` не знадобився. Арифметика меж винесена в `utils/tooltipPosition.ts` і покрита юніт-тестом, поведінка — рантаймовою перевіркою в `invariants.spec.ts`, як і вимагав цей рядок                                                                                                                                                                       | —                                                                                                                                                                                                                                                                                 |
| ~~Паритет ключів словників (I18N § 7.1)~~                                         | **закрито 2026-08-20.** Попереднє обґрунтування («типи ловлять забутий ключ») правдиве лише на третину: excess property check TypeScript застосовує до свіжого літерала, а `const translations: TranslationSchema = { common, … }` підставляє туди **імпортовані константи** — тож зайвий або з опискою ключ усередині `uk/common.ts` компілятор пропускає мовчки, як і `''`. `src/i18n-parity.spec.ts` закриває всі чотири класи: відсутні ключі, зайві ключі, порожні значення, розбіжність підстановок `{name}` | —                                                                                                                                                                                                                                                                                 |
| Онлайн-e2e у CI                                                                   | `npm run test:online` вимагає емулятора Firebase, у CI він не піднімається                                                                                                                                                                                                                                                                                                                                                                                                                                         | джоб `rules` уже піднімає емулятор із `setup-java` — онлайн-e2e додати туди ж                                                                                                                                                                                                     |
| ~~Онлайн-режим не переведено на журнал ходів~~                                    | **закрито 2026-08-18.** `handlePlayerMove` дописує хід, стан обчислюється `replayMatch()`, голоси лежать у `rooms/{id}/votes/{playerId}`, поле `gameState` прибране з кімнати цілком. `FirebaseGameStateSync` і `OnlineStateSynchronizer` видалені                                                                                                                                                                                                                                                                 | —                                                                                                                                                                                                                                                                                 |
| Покинуті кімнати ніхто не стирає                                                  | клієнт більше не видаляє чужі кімнати — це вимагало б права видаляти чуже, тобто примітиву «видалити всі кімнати». Прострочені просто не показуються в лобі                                                                                                                                                                                                                                                                                                                                                        | заплановане завдання на боці провайдера або TTL-політика. Кімната важить кілобайт, тож це не про місце                                                                                                                                                                            |
| ~~Правила доступу Firebase~~                                                      | **закрито 2026-08-18.** `npm run check:rules` тепер стоїть окремим джобом `rules` у `deploy.yml` разом із `setup-java`. 31 випадок: 10 «застосунок мусить це вміти», 21 «сторонній не мусить цього могти»                                                                                                                                                                                                                                                                                                          | —                                                                                                                                                                                                                                                                                 |
| ~~Події аналітики (ANALYTICS § 3.1)~~                                             | **закрито 2026-08-19.** `ANALYTICS_EVENTS` — реєстр, `track()` приймає `AnalyticsEvent`, не `string`. Плюс `analyticsService.spec.ts` в обидві сторони: усе надіслане є в реєстрі, і все в реєстрі надсилається (тип бачить лише перше)                                                                                                                                                                                                                                                                            | —                                                                                                                                                                                                                                                                                 |
| ~~Мова в адресі (I18N § 3.1)~~ | **знято 2026-09-02 як борг.** За `I18N-LANG-IN-PATH` (канон v9.0) сегмент вимагається лише там, де переклади індексуються; тут гра, мова — налаштування, у sitemap одна адреса. Мова живе у сховищі за правилом, а не попри нього; деталі — у розділі «Мова з адреси» нижче | — |
| ~~Чеклист бета-тестування (BETA-CHECKLIST § 5)~~                                  | **закрито 2026-08-19.** Був Markdown-файлом у `.private/docs/`, тепер дані + сторінка + 37 інваріантів. Лишається людський крок: пройти список на живих пристроях і надіслати звіт                                                                                                                                                                                                                                                                                                                                 | —                                                                                                                                                                                                                                                                                 |
| E2E ходять по dev-серверу, а не по зібраному сайту (CODE-QUALITY § 5.7)           | `webServer` у `playwright.config.ts` піднімає `npm run dev`. Різниця не косметична: у `dev` CSP приходить заголовком із nonce, у збірці — `<meta>` з хешами; порядок CSS теж інший (стилі компонентів інжектяться скриптом ПІСЛЯ `app.css`, а в збірці йдуть окремим `<link>`). Тобто зелений e2e нічого не каже про те, що поїде на хостинг                                                                                                                                                                       | заважає конкретна дрібниця: `svelte.config.js` і `vite.config.ts` беруть базу як `process.env.BASE_PATH \|\| '/MindStep'`, тож порожнім рядком її не задати, а прев'ю віддає сайт із `/MindStep/`. Перехід на `??` + `baseURL` із базою — окреме завдання з прогоном усіх наборів |
| Консоль зібраного сайту не читає жоден гейт (SECURITY § 6.2, § 15)                | заблокований ресурс не ламає розкладку й не валить тести — його видно лише як `Refused to…` у консолі, і лише на ЗІБРАНОМУ сайті. Перевірено руками 2026-08-20: консоль чиста                                                                                                                                                                                                                                                                                                                                      | e2e над прев'ю, який збирає `securitypolicyviolation`; блокує його той самий рядок, що й попередній                                                                                                                                                                               |
| `@container` не використовується жодного разу (FLUID-SIZING § 7A `FS-CONTAINER`, HIGH) | **заміряно й розібрано поштучно 2026-09-11, борг під ратчетом.** З вісімнадцяти `@media` усередині `src/lib/components/` три — про пристрій за природою (`prefers-reduced-motion` ×2, `hover: none`), шість — справді про вікно (сторінка помилки на весь екран, сітка лобі, `100vw` у вікні чату, показ десктопного ряду іконок, розкладка колонок), і **девʼять у семи файлах** мали б міряти батька. Прози більше немає: обидва переліки лежать у `src/fluid-sizing.spec.ts` (`DEVICE_QUERIES` і `CONTAINER_DEBT`), кожен запис із причиною, запит поза обома списками валить прогін, а зниклий рядок валить його з другого боку | переводити по одному файлу: `CONTAINER_DEBT` називає для кожного, де оголошувати `container-type: inline-size`. Автоматизувати не можна — поріг після переведення інший (контейнер вужчий за вікно на відступи сторінки), і число підбирається заміром у браузері |
| Десять наборів `tests/e2e/virtual/`                                               | не запускаються ніде автоматично; у CI йде лише `invariants.spec.ts` і `html-lang.spec.ts`                                                                                                                                                                                                                                                                                                                                                                                                                         | окремий джоб; наразі `@BRM-4` червоний і до цих комітів (перевірено стешем 2026-08-16)                                                                                                                                                                                            |
| ~~`createdAt` у Firestore не звіряється з `request.time` (CLOUD-DATABASE § 3.3.1)~~ | **закрито 2026-09-02.** `allow create` для `feedback/*/entries` і `rooms/*/messages` тепер вимагає `createdAt == request.time`; чат заразом отримав перелік ключів і межі довжини, яких у нього не було зовсім. Гейт перевіряє це через `documents:commit` із `setToServerValue: REQUEST_TIME` — інакше позитивний випадок віддавав би 403 на ПРАВИЛЬНОМУ правилі (прогнано обидва боки) | — |
| Доступна назва не перевіряється в рантаймі, лише в джерелах (I18N § 2) | `i18n-conventions.spec.ts` тепер бачить літерали в атрибутах, але не бачить назви, СКЛАДЕНОЇ браузером: `aria-labelledby`, текст усередині кнопки, назва, яку віддає компонент-обгортка. Саме такий випадок і був у `MenuButton` — назва йшла через проп, а не через атрибут | e2e-інваріант, що читає `accessibleName` із дерева доступності на кожній сторінці й вимагає, щоб він не збігався з `data-testid` і не був порожнім. Перекриє клас «назва є, і вона машинна», якого не бачить ні axe, ні статична перевірка |

Закрито 2026-08-16: гейти на dev-прев'ю (`deploy-dev.yml` тепер має `check`,
`lint`, `test:report`, `check:build`, `git diff --exit-code`); інваріантів над
зібраним виводом раніше не було зовсім — тепер це `npm run check:build`.

### Нові інваріанти 2026-08-19

Чотири файли перевірок, кожен із зворотним експериментом (тобто кожен доведено
червоним, а не лише зеленим — AI-AGENT-PITFALLS-v8 § 1.1):

| Файл                                             | Що тримає                                                                     | Чому саме така форма                                                                                                                                                                                                   |
| ------------------------------------------------ | ----------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/a11y-conventions.spec.ts`                   | кнопка без видимого тексту мусить мати `aria-label`                           | статична: назву чесно міряє лише браузер, але дістатися там можна лише до кнопки НА ЕКРАНІ, а половина цих живе в модалках, чаті кімнати й гілках `{#if}`. Рантаймово перевіряється головний шлях, статично — повнота  |
| `src/i18n-conventions.spec.ts`                   | у текстових вузлах шаблонів немає кирилиці                                    | латиниця не перевіряється навмисно: відрізнити `Score` від `flex-start` без розбору розмітки неможливо, а перевірка з хибними спрацюваннями закінчується вимкненою перевіркою                                          |
| `src/fluid-sizing.spec.ts`                       | вертикальні розміри від `dvh`; колонки сітки `minmax(0, 1fr)`                 | пакет називає перевіркою рівно `grep -rn "[0-9]vh" src/` — тут вона просто виконується сама. Є канарка на власну регулярку: зламана дає нуль порушень, тобто виглядає як успіх                                         |
| `src/lib/services/logService.spec.ts`            | `errorCount` — число від справжнього лічильника, `forceEnableLogging` — метод | тип не бачить, що getter повертає лічильник, а не нуль-заглушку                                                                                                                                                        |
| `src/lib/services/analyticsService.spec.ts`      | реєстр подій = те, що код надсилає, в обидві сторони                          | тип ловить опечатку в назві, але не подію, що лишилася в реєстрі після прибраного виклику                                                                                                                              |
| `tests/e2e/invariants.spec.ts` (+нова перевірка) | модалка оголошується `dialog` і має назву                                     | лише рантаймово: `getByRole('dialog')` бачить те, що бачить читалка. Доти підкладка була `role="button"`, а картка вікна — безрольовим `div`, і жоден статичний гейт цього не бачив — роль була присутня, просто не та |

**Урок цієї сесії, вартий запису.** `invariants.spec.ts` і `html-lang.spec.ts` —
це ОДИН гейт `main`, і запускати їх треба разом. Прогін лише першого дав 8/8
зелених на застосунку, який показував сторінку помилки замість головного меню:
`data-testid` на ній більше п'яти, а модалка відкривається й там. Знайшов дефект
лише другий файл — і лише тому, що він шукає конкретну кнопку.

### Нові інваріанти 2026-08-20

Аудит за пакетом v8.7. Кожен інваріант доведено зворотним експериментом
(AI-AGENT-PITFALLS-v8 § 1.1) — результат названо в тілі відповідного коміту.

| Файл                                            | Що тримає                                                              | Чому саме така форма                                                                                                                                                                                                         |
| ----------------------------------------------- | ---------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/i18n-parity.spec.ts`                       | паритет ключів словників, порожні значення, набір підстановок `{name}` | типи ловлять лише ВІДСУТНІЙ ключ: excess property check діє на свіжий літерал, а агрегатор підставляє імпортовані константи, тож зайвий ключ усередині `uk/common.ts` компілюється мовчки. `''` для типів теж валідний рядок |
| `src/css-variables.spec.ts`                     | `var(--x)` без запасного значення десь оголошена                       | `var(--немає)` не помилка: властивість стає invalid at computed-value time й мовчки бере успадковане. Знайшло шість неоголошених змінних, серед них `--unified-backdrop-filter` у семи місцях                                |
| `src/test-runners.test.ts` (+перевірка)         | кожен файл перевірки в `src/` потрапляє в маску `include`              | § 1.2: перевірка правильна, ловить свою помилку — і не входить у прогін; у виводі про неї немає ні слова                                                                                                                     |
| `src/ci.test.ts` (+перевірка)                   | є workflow, що запускається на `pull_request`                          | доти на PR не виконувалося нічого, а Dependabot відкриває їх щотижня                                                                                                                                                         |
| `src/lib/utils/tooltipPosition.spec.ts`         | арифметика меж підказки, зокрема підказка ШИРША за вікно               | порядок підтягування визначає, який кінець тексту буде обрізано; юніт-тест — єдиний спосіб це зафіксувати                                                                                                                    |
| `tests/e2e/invariants.spec.ts` (+дві перевірки) | видимий фокус у Tab-обході головної; підказка в межах вікна            | обидві лише рантаймово: `:focus-visible` залежить від того, ЯК прийшов фокус, а положення підказки — від виміру DOM                                                                                                          |

**Пастка, знайдена при написанні гейтів.** Перевірка, що читає текст файлу,
вважає порушенням ЗГАДКУ про порушення: `ci.test.ts` червонів на коментарі в
`ci.yml`, який цитував `cancel-in-progress: true`, пояснюючи, чому його там
немає. Друга того ж класу: видалення коментарів зсуває нумерацію рядків, і
перевірка звітує адреси, за якими нічого немає. Обидві полагоджені —
коментарі відрізаються, а шматки заміняються пробілами, а не зникають.

Числа боргу ESLint перезаміряні 2026-08-20 (`npx eslint . -f json`):
**341 попередження, 0 помилок** — `no-explicit-any` 166, `no-unused-vars` 121,
`no-navigation-without-resolve` 28, `require-each-key` 15, `no-restricted-imports`
9, `prefer-svelte-reactivity` 2. `ban-ts-comment` дійшов до нуля й піднятий до
`error`. Перевірок **240** у 26 файлах (`npx vitest run`), плюс 14 файлів e2e,
із яких у CI ходять два.

### Нові інваріанти 2026-08-27 (аудит за пакетом v8.11)

Прохід за всіма 37 файлами пакета. Пакет пішов уперед на чотири ревізії від
попереднього аудиту (8.7 → 8.11), і три з чотирьох знахідок цього проходу — не з
нового тексту пакета, а з прогону вже наявних гейтів проти дійсності.

| Файл | Що тримає | Чому саме така форма |
| --- | --- | --- |
| `src/css-variables.spec.ts` (+2 класи) | неколірний аргумент у `light-dark()`; `light-dark()` без `color-scheme` у тому ж блоці | канон v8.11 (`UIUX-LIGHT-DARK-COLOR-ONLY`, HIGH). Аргументи діляться ПІДРАХУНКОМ ДУЖОК: `rgba(255, 255, 255, 0.7)` сам містить коми, і наївний `split(',')` мовчки рахував би пару непокритою. Нерозібране значення — червоне, а не пропуск: тихий пропуск це рівно те, через що в `as5` сім мертвих токенів жили при 207 зелених тестах |
| `src/ui-conventions.spec.ts` | `color-scheme` ≠ `light`; try/catch в інлайн-скрипті; емодзі в словниках; сирий `<svg>` у маршрутах | UI-UX § 4 — чотири інваріанти, яких не було жодного. Емодзі перевіряються по СЛОВНИКАХ, а не по компонентах: там живуть user-facing тексти, а в компонентах символ буває законним |
| `src/hotkeys-conventions.spec.ts` | слухач keydown рівня вікна кличе `services/keyboard`; `V`/`R` не зайняті звичайною дією; призначення записані як `code` | захисти були написані, покриті тестами й НЕ обовʼязкові — обробник повз них проходив усі гейти. Винятків два, обидва з причиною, і окремий інваріант стереже, щоб виняток не пережив свою причину |
| `src/ci.test.ts` (+перевірка) | у кожному job зі збіркою є `git diff --exit-code`, і саме ПІСЛЯ неї | крок зник із `deploy.yml`, лишивши по собі коментар, приліплений до сусіднього кроку |
| `tests/e2e/a11y.spec.ts` + `tests/a11y-baseline.ts` | машинний аудит axe: 8 сторінок + відкрита модалка | найбільший записаний борг проєкту. Поріг — база, що лише спадає, ПЛЮС перелік `id`: нуль червонів би від оновлення axe-core без зміни коду, а такий гейт вимикають |
| `src/css-variables.spec.ts` (+4-й клас) | колірна властивість не залежить від змінної, яку не оголошено НІДЕ | межа першого класу: із запасним значенням поведінка визначена — доки змінна десь оголошена. Коли її немає ніде, «запасне» стає ЄДИНИМ, тобто літералом, замороженим під одну тему. Знайшло 11 змінних і 25 вживань |
| `src/contrast.spec.ts` + `src/contrast-baseline.ts` | контраст усіх дванадцяти комбінацій палітри | axe міряє лише ту тему, яка застосована; решту одинадцяти не бачив ніхто. Пари беруться З КОДУ, не з переліку: `--confirm-btn-bg`/`--confirm-btn-text` виглядають парою й не вживаються разом жодним правилом. Пропуски пораховані числом |
| `scripts/check-build.mjs` (+перевірка) | у бандлі немає `PRIVATE_KEY`, `SERVICE_ACCOUNT`, `CLIENT_SECRET`, PEM-заголовка | у static-профілі межі між конфігом і секретом у Vite немає; сім `VITE_FIREBASE_*` уже їдуть у бандл, і наступна змінна поруч стала б непомітною |

**Три дефекти, знайдені гейтами цього проходу.**

1. **`a11y-conventions.spec.ts` не бачив 58 кнопок зі 110.** Розбір тега стояв на
   регулярці `/<button\b([^>]*)>…/`, а `[^>]*` зупиняється на першому `>` — і
   найчастіший атрибут кнопки в Svelte це `onclick={() => …}`, де стрілка містить
   `>`. Усе після зрізу потрапляло у «вміст кнопки», а літери там є завжди, тож
   кнопка вважалася підписаною. Гейт звітував нуль порушень; точний розбір знайшов
   16 безіменних кнопок, серед них кнопка перезапуску на екрані аварії — єдиний
   працездатний елемент керування в момент, коли все інше зламалося. Полагоджено
   всі 16, розбір переписаний посимвольним сканером, поруч дві канарки на САМ
   РОЗБІР (по результату помилку не було видно ніяк).

2. **Крізь згорнуте меню ходив Tab.** `FlexibleMenuPanel` ставив
   `aria-hidden={!isOpen}` на сітку меню, а згорнуте меню не зникає — воно їде за
   екран `transform: translateX(-100%)`, і кнопки лишаються справжніми `<button>`.
   Разом це найгірший стан: людина з клавіатури проходить крізь пʼять кнопок,
   яких не видно, а читалка про них мовчить. Замінено на `inert`. Знайшов axe
   (`aria-hidden-focus`) на семи сторінках із девʼяти.

3. **`Ctrl+Escape` закривав модалку.** `BaseModal` перевіряв рівно
   `e.key === 'Escape'` без модифікаторів, тобто застосунок реагував на
   комбінацію, що належить системі. Переведено на `acceptsShortcut` — той самий
   модуль, який `keyboard.ts` у власній шапці й називає спільним.

**Четвертий дефект, знайдений розвʼязувачем контрасту.** Одинадцять змінних не
оголошені НІДЕ, і їхнє «запасне» значення було не запасним, а єдиним — літералом,
замороженим під темну тему. У світлій це давало напис, якого не видно:
центральна кнопка меню — білий текст на `#e0e1e6` (**1.31:1**), кнопка
`.action-btn.secondary` на екрані аварії — темний текст на темному тлі
(**1.03:1**). Кожне вживання переведене на токен, який у палітрі вже є, зі
збереженням того самого літерала запасним; оголосити одинадцять нових імен
означало б подвоїти половину палітри синонімами.

**Два борги, записані числом замість виправлення.** `tests/a11y-baseline.ts`
тримає два `color-contrast`, обидва заміряні: «Очистити кеш повністю» — `#fff` на
`#f44336`, **3.69:1** (17.6 px bold, тобто НЕ великий текст, поріг 4.5:1);
«Почати гру» — `#fff` на `#4caf50`, **2.66:1**. Джерело одне — білий текст на
середньому за світлістю фірмовому кольорі. Це рішення про палітру (шість стилів ×
дві теми), і канон § 10.1.1 для цього й тримає базу. Кандидат заміряний:
`#d32f2f` дає **5.05:1** із тим самим білим, і саме це значення вже стоїть у
`:root` — світлішим його перекривають темні теми (`base/variables.css`, рядок 146).

**Виняток гейта axe, названий поіменно.** `NetworkMonitorWidget` живе під
`{#if import.meta.env.DEV}` і до відвідувача не доїжджає, але дає axe три
порушення на кожній сторінці: `.header` має `role="button"` і містить дві
справжні кнопки (`nested-interactive`), а самі кнопки менші за 24 px
(`target-size` ×2). Виняток адресний — один `data-testid`. Записати їх у базу
означало б СХОВАТИ справжній `nested-interactive`, якби він зʼявився в тому, що
відвантажується. Зникне сам, коли e2e переїдуть на зібраний сайт.

**ESLint: `no-restricted-imports` піднято до `error`.** Борг дійшов до нуля
2026-08-23, і рядок пішов із мапи `DEBT`, але саме правило лишилося `warn` із
коментарем «9 знахідок». Наслідок вимірний: повернутий `writable` у Svelte-5 коді
давав ЗЕЛЕНИЙ `npm run lint`, а червонів лише прогін тестів — і то не як
«заборонений імпорт», а як «правило дає попередження без записаного числа».
CODE-QUALITY § 6.4.1 називає це прямо.

**Розмір боргу палітри названий числом.** Розвʼязувач знайшов пари нижче порогу
в дванадцяти комбінаціях (скільки саме — рядок `contrast-debt` у «Числах під
гейтом»), і перелік лежить у `src/contrast-baseline.ts`. Це не «додати гейт і
забути»: рядок прибирається лише разом із виправленням пари, і гейт червоніє в
обидва боки — борг, що скоротився й не прибраний, теж червоний.

Борг ESLint на цей прохід — мапа `DEBT` у `src/eslint-baseline.test.ts`: там
кожне правило зі своїм числом, і гейт звіряє їх на РІВНІСТЬ в обидва боки.
Другої копії тут немає навмисно (§ 5.5.1): дублікат рветься саме тому, що він
другий. Обсяг прогону — рядки `unit-spec-files`, `e2e-spec-files` та `e2e-in-ci`
у «Числах під гейтом».

### Нові інваріанти 2026-09-02 (аудит за пакетом v8.12)

Пакет пішов уперед на одну ревізію (8.11 → 8.12), і ця ревізія додала не
уточнення, а **цілу тему**: реєстри даних, розгортання атрибутів під CSP, кінці
рядків, бюджет код-проти-даних, застарілі числа в прозі. Прохід закрив три з
чотирьох застосовних нових правил гейтами й знайшов чотири дефекти, яких не
бачила жодна наявна перевірка.

| Файл | Що тримає | Чому саме така форма |
| --- | --- | --- |
| `.gitattributes` + `src/eol.test.ts` | `* text=auto eol=lf`; ні індекс, ні робоче дерево не мають CRLF | `PIT-EOL-GATE` (HIGH). Файли перевірок у `src/` (їх число — рядок `unit-spec-files`) читають власні джерела ТЕКСТОМ, і при CRLF дають інший вердикт: `.` не збігається з `\r`, `$` без `m` стоїть перед `\n`, але не перед `\r`. Найгірший прояв — мовчазний нуль знахідок. Перевіряється СТАН обох колонок `git ls-files --eol`, а не наявність файлу |
| `src/doc-numbers.test.ts` + розділ «Числа під гейтом» | кожне число, яким цей документ описує поточний стан | `PIT-NUMBER-UNDER-GATE` (HIGH). Звірка в обидва боки: ключ, прибраний із таблиці, валить прогін так само, як розбіжність числа |
| `scripts/check-build.mjs` (+бюджет) | код критичного шляху, CSS і дані — трьома окремими числами | `PERF-BUDGET-CODE-VS-DATA` (MEDIUM) + `GATE-BUNDLE-BUDGET`. Бюджету не було зовсім; одне сумарне число рахувало б словники i18n разом із кодом. Маркер словника має перевірку на існування: його зникнення означає мертвий гейт, а не «даних не стало» |
| `src/i18n-conventions.spec.ts` (+2 класи) | доступна назва (`aria-label`, `title`, `placeholder`, `alt`) не буває літералом — ні прямим, ні всередині виразу | I18N § 2. Тут перевіряється ЛІТЕРАЛ, а не кирилиця: у цих атрибутах значення завжди для людини, тож англійське «Decrease» ловиться так само, як українське. Рідкий випадок, коли суворіше правило дає МЕНШЕ шуму |

**Чотири дефекти, знайдені цим проходом.**

1. **659 файлів робочого дерева з CRLF проти LF в індексі.** Ціну тут уже
   платили двічі й обидва рази лікували наслідок: хеш CSP над CRLF мовчки
   вимикав скрипт теми, а розбір кроків workflow бачив НУЛЬ кроків на
   Windows-чекауті (докблок `src/ci.test.ts` описує це власними словами).
   Причина лежала на рівні репозиторію, і доки вона там, нормалізувати довелося
   б у кожному новому гейті окремо.

2. **Вісім із дев'яти чисел документа розійшлися з дійсністю.** Гейт правил бази
   виріс із 54 випадків до 83, а борг палітри, навпаки, СКОРОТИВСЯ з 46 до 41 —
   тобто документ приховував не лише недоробку, а й зроблену роботу.

3. **Шістнадцять декоративних `<svg>` мали власну доступну назву**, одинадцять із
   них українською в усіх чотирьох мовах. Кожна лежала ВСЕРЕДИНІ елемента
   керування, який уже названий правильно, тобто в кнопці був другий названий
   вузол із суперечливою назвою. Дефект жив рівно в проміжку між трьома
   перевірками: axe питає, чи назва Є; `a11y-conventions.spec.ts` — чи вона не
   ВІДСУТНЯ; `i18n-conventions.spec.ts` дивився текстові вузли, а не атрибути.

4. **`MenuButton` віддавав читалці машинний ідентифікатор**: «main-menu-link,
   кнопка», «test-mode-btn, кнопка» — при тому, що людська назва в кожної кнопки
   була, у полі `tooltip`, і йшла у візуальну підказку поруч. Плюс зашиті рядки в доступних
   назвах — від назви КОЖНОЇ клітинки дошки до кнопок відтворення, у яких
   переклад лежав у словнику всіх чотирьох мов і просто не використовувався.

**Дефект, який знайшли самі виправлення.** Переклад назви кнопки розгортання
меню поклав застосунок цілком: `FlexibleMenu` монтується разом із кореневим
layout, тобто ДО того, як `svelte-i18n` отримає початкову локаль, і перший `$t`
кидає «Cannot format a message without first setting the initial locale». Це той
самий симптом, який `+layout.svelte` уже описував для `NetworkMonitorWidget`, —
і саме e2e його знайшов, бо в браузері головного меню не було взагалі. Обидва
`FlexibleMenu` тепер під `i18nReady.ready`; `LogCopyButton` із тієї ж причини
лишився БЕЗ словника й записаний у виключення гейта з поясненням.

**Два хибні спрацювання, полагоджені разом із гейтом.** Обидва — той самий клас,
що вже ловився в `a11y-conventions.spec.ts` (`[^>]*` зупиняється на першому `>`):
вираз атрибута, взятий регуляркою `\{[^}]*\}`, обрізався на `}` усередині
`$t("key", { color })`, і ключ читався як зашитий текст; шаблонний рядок віддавав
`${nameOf(style)}` за текст. Виправлено звіркою дужок і вирізанням підставлень,
на кожне стоїть окрема канарка — хибне спрацювання є найкоротшим шляхом до
вимкненої перевірки (CODE-QUALITY § 6.4.1).

### Нові інваріанти 2026-09-10 (аудит за пакетом v9.2)

Пакет пішов уперед на цілу мажорну ревізію (8.12 → 9.2) і додав шість нових
гейтів: `GATE-STRUCTURE`, `GATE-SVELTE-SOURCES`, `GATE-A11Y-STATIC`,
`GATE-STALE-BUILD`, `GATE-E2E-IDENTITY`, `GATE-LIGHTHOUSE`. З них
`GATE-A11Y-STATIC` у проєкті вже був (канон називає його джерелом саме цей
репозиторій), `GATE-LIGHTHOUSE` був частково; решта — ні.

Прохід закрив вісім застосовних нових правил і знайшов **шість дефектів**, яких
не бачила жодна наявна перевірка.

| Файл | Що тримає | Чому саме така форма |
| --- | --- | --- |
| `src/lib/services/ownScope.ts` + `src/own-scope.spec.ts` | `getRegistrations()` і `caches.keys()` не викликаються без фільтра «своє» | `DBG-HARD-RESET` (CRITICAL). Дефект — у ВІДСУТНОСТІ фільтра, тож перевірка по джерелах: тест поведінки довелося б писати на кожен виклик окремо, і четвертого, доданого завтра, він не побачив би |
| `src/stale-build.test.ts` + `check-build.mjs` | `skipWaiting`/`clientsClaim` у конфігу й у зібраному воркері; `registerType: 'prompt'`; пропозиція оновитися існує й змонтована | `VER-OPEN-TAB-SURVIVES` (HIGH). Два шари, бо конфіг і артефакт — різні твердження: `skipWaiting()` у воркер вставляє генератор, і конфіг може бути чистим, а артефакт ні. У зібраному воркері дозволений лише виклик в обробнику `SKIP_WAITING` — і на вирізання цього обробника стоїть канарка |
| `scripts/check-test-discovery.mjs` (+ крок у трьох workflow) | кожен файл перевірки належить раннеру й потрапляє в його маску | `PIT-TEST-DISCOVERY-PROCESS` (MEDIUM). ОКРЕМИЙ ПРОЦЕС, бо перевірка стежить за маскою `test.include` — і звуження маски викидає з прогону її саму. `src/test-runners.test.ts` лишився другим прогоном тієї самої функції плюс двома твердженнями, яких скрипт про себе зробити не може: що він існує і що його хтось кличе |
| `src/structure.test.ts` | досяжність графом імпортів; руни лише у `.svelte`/`.svelte.ts`; псевдонім = імʼя файлу; ратчет розміру зі стелею на кожен файл | `PS-REACHABILITY` (HIGH), `PS-SIZE-RATCHET` (MEDIUM). Граф, а не гріп по імені: гріп не бачить ЛАНЦЮЖКА сиріт. `new URL(…, import.meta.url)` розбирається окремо, інакше `ai.worker.ts` виглядав би сиротою; `.js` теж у графі — без двох дій у `src/lib/actions/` не розвʼязувалося 19 імпортів |
| `src/flags-registry.spec.ts` | `FLAG_COUNTRIES` = `static/flags/*.svg` у обидва боки | `PS-STATIC-ORPHANS` (MEDIUM). Генератора тут немає навмисно: набір збирає `npm run sync:flags` у сусідньому `VetCrewGames`, тобто оновлення — ручна копія ДВОХ речей, а такі операції ламаються наполовину |
| `src/doc-facts.test.ts` | шляхи в документах резолвяться двобічно; «npm run <скрипт>» існує; версія пакета однакова в усіх документах | `PIT-DOC-FACTS` (HIGH). Резолвер, а не `existsSync`: документи згадують файли коротким імʼям, і перевірка «шлях від кореня» червоніла б на восьми десятках правдивих згадок. Дефект на обох краях: нуль кандидатів — згадка в нікуди, більше одного — згадка неоднозначна |
| `src/svelte-sources.spec.ts` | пара до `addEventListener`/`setInterval`/`observe()`; `structuredClone`/`postMessage` без знімка; підписка, яку кличе лише сусідній сервіс | `SC-LISTENER-CLEANUP` (MEDIUM), `SC-SNAPSHOT-BOUNDARY` (HIGH), `SC-SUBSCRIPTION-WIRED` (HIGH). Замінює прибраний live-subscriptions.spec.ts, який перевіряв ОДИН файл, названий рядком; споживач мусить бути ПОЗА `src/lib/services/`, бо `roomService.subscribeToChat` делегує в однойменний метод шару нижче |
| `src/ci.test.ts` (+2 розділи) | мажор кожної дії — з переліку, перевіреного людиною; між збіркою й викладанням ніщо не переписує `build/` | `CI-ACTION-RUNTIME` (MEDIUM), `CI-DEPLOY-ORDER` (HIGH). Мережі в прогоні немає, тож перелік мажорів на `node24` — це те, що людина подивилася в `action.yml`, із датою. Порядок кроків: `Preserve dev folder` названий винятком явно, бо дописує `build/dev/`, не торкаючись перевіреного |
| `tests/e2e/reflow.spec.ts` | WCAG 1.4.10: на 320 CSS px немає горизонтальної прокрутки | `A11Y-REFLOW` (MEDIUM). axe цього не бачить у принципі — це геометрія, а не розмітка. Перелічуються ВИННІ вузли, а не лише факт; вміст усередині власного `overflow-x` виною не вважається |
| `src/i18n-parity.spec.ts` (+3 інваріанти) | кожен файл локалі підключений до свого агрегатора й не до чужого | `I18N-LAZY-CHUNK-PARITY` (HIGH). Другий бік рішення «словники імпортуються, а не читаються з диску»: паритет бачить лише імпортоване |
| `src/ui-conventions.spec.ts` + `check-build.mjs` | у кожного мета-тега один власник | `SEO-HEAD-SINGLE-OWNER` (HIGH). Другий шар обов'язковий саме тут: SSR вимкнений, тож у `build/` немає нічого зі `<svelte:head>`, і дубль поверх `app.html` перший шар не побачить ніколи |

**Шість дефектів, знайдених цим проходом.**

1. **Критичне оновлення знімало service worker і кеші сусідніх сайтів.**
   `appInitializationService.performHardReload()` ходив по всьому origin двома
   циклами без фільтра, тоді як `maintenanceService.hardReset()` фільтрував — із
   докблоком, який навіть називав сусіда, що робить це неправильно. Тобто
   правильний фільтр був написаний один раз і не поширився на наступні виклики.
   Третє місце того самого класу — прибирання старого sw.js у `+layout.svelte`
   за умовою `scriptURL.endsWith('/sw.js')`, якій відповідає й сусідський
   `…/Slovko/sw.js`.

2. **Воркер забирав відкриту вкладку, а готова пропозиція оновитися була мертвим
   кодом.** `registerType: 'autoUpdate'` разом зі `skipWaiting: true` і
   `clientsClaim: true` означають, що новий воркер не чекає — тож `needRefresh`
   не ставало `true` ЖОДНОГО РАЗУ, і `ReloadPrompt.svelte` із двома кнопками не
   показувався. Канонічний § 4.4 у проєкті був написаний і вимкнений прапорцем у
   сусідньому файлі.

3. **Перевірка «кожен тест справді запускається» зникала разом із тестами.**
   Заміряно: зі звуженою маскою `src/**/*.spec.ts` `vitest` друкує «332 passed»
   без жодного попередження, а з прогону випадають десять файлів — серед них
   гейти CSP, кінців рядків, чисел документа й базового набору ESLint.

4. **Сирота, якого гріп «знаходив».** Прибраний src/lib/services/analytics.ts — один
   рядок `export * from './analyticsService'` без жодного імпортера, при
   десятках збігів на гріп по слову `analytics`. `knip` у проєкті стоїть, але
   не викликається в жодному workflow, і з його шести «unused files» п'ять були
   хибними.

5. **Обробник видимості реєструвався анонімною функцією**, тобто зняти його було
   неможливо в принципі, а `cleanup()` знімав лише інтервал версії. Плюс
   `setInterval` у `ReloadPrompt.svelte` без пари.

6. **Чотири сталі факти в трьох файлах.** Відхилення `CDB-GATE-SERVER-TIME`
   пережило свою причину на кілька годин (записане 2026-09-02 «звірки з
   `request.time` немає» — наступний коміт того самого дня додав дві й гейт до
   них); `AGENTS.md` тричі вів на logService.ts замість
   `logService.svelte.ts`; `README.md` називав `npm run test:unit`, якого не
   існувало, і вів у selection_criteria/v8 при шапці на v9;
   `countries.generated.ts` починався з «ГЕНЕРОВАНО `npm run sync:flags`» —
   скрипта, якого в цьому проєкті немає, — і називав 265 назв при 262 файлах.

**Два дефекти, знайдені самими перевірками в собі — і обидва варті рядка.**

Перша версія гейта reflow дала чотири червоні сторінки з переконливими числами
(`/rules` 384 проти 305, `/settings` 410, `/local-setup` 406, `/rewards` 322).
Виявилося, що вона міряла НЕПЕРЕКЛАДЕНУ сторінку: доки `svelte-i18n` не віддав
локаль, `$t` малює сам ключ, а `mainMenu.clearCacheModal.keepAppearance` — рядок
без пробілів, тобто нерозривний, і розсуває розкладку сильніше за будь-який
справжній текст. Після очікування на переклад усі вісім сторінок зелені:
горизонтальної прокрутки на 320 px у проєкті немає.

Канарка Lighthouse, яку приписує `OBS-LHCI-REAL-PAGES`
(`resource-summary:stylesheet:count` із `minNumericValue: 1`), тут **не може
почервоніти**: прибрано всі одинадцять `<link rel="stylesheet">` із
`build/index.html`, зібрано заново — Lighthouse нарахував шістнадцять стилів і
асерція лишилася зеленою, бо CSS у SPA-фолбеку тягне клієнтський завантажувач
через JS. Асерцію знято, причину записано в `lighthouserc.cjs`, а те саме
твердження перевіряється над артефактом: кожна адреса `_app/`, яку просить
документ, існує від тієї точки монтування, з якої її попросять.

**Два хибні спрацювання, полагоджені разом із гейтами.** Обидва — той самий
клас «докблок цитує заборонене», що вже ловився в `test-runners.test.ts` і
`ci.test.ts`: `svelte-sources.spec.ts` оголосив витоком `ReplayViewer.svelte`,
де `setInterval` згадується двічі в коментарі про те, чому його звідти прибрали,
а гейт власника мета-тегів оголосив порушником саме ту сторінку, яка робить усе
правильно — ліниве `[\s\S]*?` зачепилося за `<svelte:head>` у докблоці й
проглинуло весь файл. На кожне вирізання коментарів тепер стоїть канарка з
точною ознакою захвату.

### Нові інваріанти 2026-09-11 (другий прохід за пакетом v9.2)

Прохід за тими самими 30 файлами пакета, що й 2026-09-10, але з іншим питанням:
не «чи є правило в коді», а «що покаже гейт, якщо його справді запустити».
Знайдено **вісім дефектів**, і чотири з них знайшла не людина й не греп, а
щойно написана перевірка — включно з двома, що вказали на дірки в перевірках
попереднього проходу.

| Файл | Що тримає | Чому саме така форма |
| --- | --- | --- |
| `tests/e2e/identity.setup.ts` + проєкт-залежність у `playwright.config.ts` | на тестовому порті саме цей проєкт (`<meta name="application-name">`) і саме це робоче дерево (`/version.json` проти `package.json`) | `CI-E2E-TARGET-IDENTITY` (MEDIUM). Playwright дивиться на порт РІВНО ОДИН РАЗ, до запуску команди; доки dev-сервер компілює модулі, сусід із цієї ж машини встигає його зайняти. Саме проєкт-залежність, а не тест: інакше червонів би один файл, а решта перевіряла б чужий сайт. Перевірено: залежність виконується й тоді, коли прогін фільтрують іменами файлів |
| `scripts/check-test-discovery.mjs` (+2 класи) | файл у `testDir` поза типовою маскою Playwright, якого не назвав жоден `testMatch`; setup-проєкт, від якого не залежить ніхто | сканер шукав рівно два суфікси й `identity.setup.ts` не бачив ЗОВСІМ — тобто перший же файл нового класу проліз повз перевірку «кожен тест справді запускається». Розбір конфігу структурний, по дужках: греп зіставив би `testMatch` одного проєкта з `name` іншого |
| `tests/wait-for-ui.ts` (спільний гард на три гейти) | розмітка є, переклад приїхав, кількість інтерактивних цілей перестала рости | заміряно: на `/controls` одразу після старого гарда в DOM **нуль** `.key-button`, а через дві секунди — **44**. Ознака «лічильник не змінився за 400 мс» не знає про сторінки нічого; чекати на конкретний локатор довелося б дописувати щоразу, і саме цього ніхто не зробить |
| `tests/e2e/touch-targets.spec.ts` + `tests/touch-targets-baseline.ts` | пари сенсорних цілей: фактичне перекриття > 4 px і те, що станеться при рості ОДНІЄЇ цілі до 44×44 | `A11Y-TOUCH-OVERLAP` (MEDIUM). Виняток виражений властивістю, а не переліком селекторів: шар цілі — найближчий предок із `position: fixed`. Саме ЕЛЕМЕНТ-шар, а не прапорець «плаває»: на `/`, `/join` і `/controls` уся сторінка лежить у фіксованій обгортці, і з прапорцем поділ переставав ділити. База — перелік ПАР, зараз порожній на всіх восьми сторінках |
| `src/testid-conventions.spec.ts` (+4 твердження) | проп, з якого виходить локатор, не має порожнього типового значення | `BETA-TESTID-REQUIRED`. Тримає ПРИЧИНУ: без типового значення проп стає обовʼязковим, і повнотою займається `svelte-check` — на КОЖНОМУ місці виклику, а не на восьми сторінках, куди дійшов e2e |
| `src/fluid-sizing.spec.ts` (+`DEVICE_QUERIES`, `CONTAINER_DEBT`) | кожен `@media` у компоненті віднесений або до пристрою з причиною, або до боргу з майбутнім контейнером | `FS-CONTAINER` (HIGH). Заборони немає навмисно: частина запитів правильна, а обійдену перевірку не читають. Ратчет в обидва боки — рядок, що лишився після переведення, валить прогін так само, як новий запит |
| `src/hotkeys-conventions.spec.ts` (+3 твердження) | кожна одиночна літера в контексті `global` названа в записі про WCAG SC 2.1.4 | `HK-WCAG-CHARACTER-KEY` (CRITICAL) і ще чотири `HK-*` вимагають запису в цьому файлі. Тримається не «критерій виконано» — цього статично не довести, — а ПЕРЕЛІК. Читається один рядок, а не весь розділ: у таблиці поруч названі ігрові `KeyM` і `KeyI` |
| `src/csp-hash.test.ts` (+3 твердження) | розгортання `{...obj}` на `img`/`iframe`/`video`/`audio`/`source`/`embed`/`object`; SSR лишається вимкненим | `SEC-CSP-SPREAD-HANDLER` (HIGH). Гейта над `build/` тут бути не може (розмітки компонентів там немає), тож перевіряється ПРИЧИНА, видима в джерелах. Тригер відхилення став червоним тестом з інструкцією замість абзацу |
| `src/ci.test.ts` (+1 розділ) | кожен `.spec.ts` верхнього рівня `tests/e2e/` названий у команді workflow | перелік там поіменний навмисно (`virtual/` важкий, `online/` потребує емулятора), і ціна цього рішення — саме те, що доданий файл легко не вписати: прогін зелений, файл не виконується |

**Вісім дефектів, знайдених цим проходом.**

1. **Три гейти міряли сторінку керування без жодної клавіші на ній.** Гард був
   `[data-testid]` більше пʼяти; на `/controls` після нього — нуль
   `.key-button`, через дві секунди — 44. Тобто axe, reflow і новий гейт
   сенсорних цілей дивилися на порожню сторінку. Знайшлося випадково і саме
   тому варте запису: гейт сенсорних цілей один раз показав 40 пар «клавіша ×
   хрестик», а наступні чотири прогони — жодної.

2. **Невидимий хрестик ловив дотик у кутку кожної клавіші.** `.remove-key-btn`
   висить на `top: -8px; right: -8px` і перекриває клавішу на 12×12 px, а мав
   лише `opacity: 0` — тобто був невидимий, але ловив події завжди. На дотику
   наведення не буває: побачити його на телефоні не міг ніхто, а натиснути —
   випадково, цілячись у кут клавіші. Плюс клавіатурою він стояв у Tab-обході з
   невидимим фокусом.

3. **Локатор, що приходить пропом, виходив безіменним.** `EditableText`
   складає `data-testid="{dataTestId}-edit-btn"`, проп мав типове `""`, і на
   `/rewards` його не передавали — у DOM жили `-edit-btn` і `-random-btn`.
   Другий бік того самого: `GameModeWidget` мав дві групи кнопок узагалі без
   локатора на контейнері.

4. **Дві іконкові кнопки відбирали дотики одна в одної.** 26 px при проміжку
   4 px: зона однієї, розширена до 44×44, залазить на сусідню на 5 px там, де
   візуально не змінилося нічого.

5. **Перевірка «кожен тест справді запускається» не бачила нового класу
   файлів.** `identity.setup.ts` — перший `.setup.ts` у проєкті, і сканер, що
   шукає рівно `.spec`/`.test`, проліз би повз нього мовчки.

6. **Документ стверджував відповідність WCAG рівня A, якої для двох клавіш
   немає.** `KeyT` і `KeyL` зашиті в `+layout.svelte`: їх не можна ні
   вимкнути, ні перепризначити, а коментарі в коді казали «шлях 2». Обраний
   шлях тепер записаний, разом із названим боргом і двома кандидатами на
   закриття.

7. **Тригер відхилення був наполовину виконаний із самого початку.** Запис
   казав «переглянути при увімкненні SSR **або prerender**», а
   `src/routes/+page.ts` має `prerender = true` увесь час — із вимкненим SSR це
   той самий порожній каркас. Умова, яка вже виконана, тригером не буває.

8. **Числа в таблиці «Числа під гейтом» розійшлися з дійсністю в тому ж
   комітi, що їх змінив** — `e2e-spec-files` і `e2e-in-ci`. Знайшов це
   `GATE-DOC-NUMBERS`, тобто гейт спрацював так, як задумано; коміт
   виправлено до того, як він поїхав далі.

**Три хибні спрацювання, полагоджені разом із гейтами** — і всі три того самого
класу «перевірка вважає ЗГАДКУ про порушення порушенням», що вже ловився в
`test-runners.test.ts` і `ci.test.ts`. Докблок у `ButtonGroup.svelte`, який
ПОЯСНЮЄ, чому порожній локатор поганий, сам містив приклад — і перевірка
оголосила порушником саме той файл, який щойно полагодили. Закоментований
`// hotkeyService.register("global", "KeyI", …)` у `TopRowWidget.svelte` зробив
`KeyI` «глобальною літерою», якої в застосунку немає. Докблок у `Flag.svelte`,
що пояснює вибір `<img>`, з’їв перший зворотний експеримент над розгортанням
атрибутів. На кожен випадок стоїть канарка.

**Чого цей прохід НЕ зробив, і чому.** Переведення девʼяти `@media` на
`@container` і закриття 2.1.4 для `T`/`L`. Перше змінює вигляд (поріг після
переведення інший — контейнер вужчий за вікно на відступи сторінки, і число
підбирається заміром у браузері), друге додає рядок UI й чотири переклади.
Обидва тепер названі поіменно й стоять під ратчетом — це робота під очима, а не
вночі.

## Локальні пастки проєкту

- **`npm test` — це не vitest.** `npm test` запускає `scripts/robust-test.js`;
  юніт-тести — `npm run test:report` (той самий `vitest run`, який CI і викликає).
  Агент, який побачить «test» і зробить висновок про юніт-тести, помиляється.
- **Онлайн-тести потребують емулятора Firebase:** `npm run test:online` піднімає
  його сам (`firebase emulators:exec`), решта e2e — ні.
- **`base` різний у dev і build.** У `dev` він порожній, у збірці — `/MindStep`.
  Будь-який захардкоджений `/MindStep/...` у коді зламає локальну розробку, і
  навпаки.
- **Префікс сховища — з одного місця.** `config/storage.ts` (`STORAGE_PREFIX`,
  `getStorageKey()`); `services/storage.ts`, `storageMigration.ts`,
  `maintenanceService.ts` і `utils/cacheManager.ts` беруть його звідти. Хардкод
  `'mindstep_'` лишився лише в інлайновому скрипті `app.html` — він мусить
  виконатися до гідратації.
- **CSP уже налаштована й чутлива до Firebase.** `svelte.config.js` перелічує
  `*.googleapis.com`, `*.firebaseio.com`, `*.firebasedatabase.app`, `wss://` для
  Realtime Database і `localhost`/`127.0.0.1` для емуляторів. Новий сторонній
  домен без правки `connect-src` блокується мовчки.
- **Хеш інлайн-скрипта рахується з тексту, нормалізованого до LF.** `app.html`
  збережений із CRLF, а парсер HTML замінює CRLF на LF ще до появи DOM — тобто
  браузер хешує НЕ те, що лежить у файлі. Хеш «як у файлі» не збігається ніколи,
  і скрипт теми мовчки блокується: сторінка рендериться правильно, просто тема
  мигає. `svelte.config.js` і `scripts/check-build.mjs` роблять `.replace(/\r\n/g,
'\n')` саме тому. Це коштувало одного хибного «перевірено» 2026-08-16.
- **У `vite dev` консоль лається на CSP — і це не дефект застосунку.** У
  режимі розробки Vite додає власні інлайн-скрипти, яких немає в хеші з
  `svelte.config.js`, а сам шаблон він кешує окремо від конфігу; після правки
  `app.html` політика і сторінка розходяться до перезапуску. Симптом —
  `Executing inline script violates…` на кожній сторінці. На ЗІБРАНОМУ сайті
  консоль порожня (перевірено 2026-08-20 на `npm run preview`), і саме він тут
  джерело правди (AI-AGENT-PITFALLS-v8 § 2). Не «лагодити» це послабленням
  політики.
- **`data-theme` НЕ доводить, що анти-FOUC-скрипт виконався.** Той самий атрибут
  виставляє контролер теми після гідрації. Доказ — порожній перелік
  `securitypolicyviolation` на зібраному сайті, а не наявність атрибута.
- **Емулятори Firebase лишають java-процеси на 8080/9000/9099.** Після падіння
  `emulators:exec` наступний запуск каже «port taken», і це виглядає як чужий
  сервіс. Знімаються `Get-NetTCPConnection -LocalPort 8080,9000,9099 -State
Listen | ForEach-Object { Stop-Process -Id $_.OwningProcess -Force }`.
- **`<html lang>` виставляє `svelte-i18n`, а не код проєкту.** У `src/` немає
  жодного рядка, який це робить, — атрибут оновлює підписка бібліотеки на
  `locale`. Не «полагоджуй» це власною синхронізацією: буде два механізми на одну
  річ. Стежить за цим `tests/e2e/html-lang.spec.ts`.
- **`export let` у проєкті більше немає — нуль** (заміряно
  `grep -rl 'export let ' src --include=*.svelte | wc -l`, 2026-08-20; було 13,
  потім 1). Останнім лишався `Tooltip.svelte`, і лишався не з недогляду: його
  `$:` не обчислює значення, а МІРЯЄ DOM і з виміру виводить позицію, тож
  прямолінійний переклад дав би `$effect` із записом у `$state` — сам по собі
  HIGH за SVELTE-CORE-v8 § 1.2. Розв'язок — `bind:offsetWidth`/`bind:offsetHeight`:
  вимір веде Svelte, позиція стає `$derived`. **Якщо колись знадобиться
  виміряний розмір — це той самий шлях, а не `$effect`.**
  `createEventDispatcher` і директив `on:` на компонентах теж немає — нуль.

## Свідомі відхилення від пакету

| Правило пакету                                                  | Відхилення                                                                                                                                                                                                                                                                                             | Обґрунтування                                                                                                                                                                                                                                                                      |
| --------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `no-var` (CODE-QUALITY)                                         | лишається у `**/*.d.ts`                                                                                                                                                                                                                                                                                | `declare var` — єдиний спосіб оголосити глобал; вимкнено в `eslint.config.js` для `.d.ts`                                                                                                                                                                                          |
| «ігнорування лише з коментарем» (SVELTE-UI § 3)                 | один `eslint-disable` без «виправлення»                                                                                                                                                                                                                                                                | порожній шаблон `({}, testInfo)` — те, чим Playwright статично розуміє «фікстури не потрібні». Другий (початкові нулі в `Tooltip.svelte`) зник разом із `$:`-блоком 2026-08-20                                                                                                     |
| скорочені префікси сховища (STORAGE § MEDIUM)                   | `mindstep_`, а не `mindstep.odesa.ua_`                                                                                                                                                                                                                                                                 | це не домен, а назва проєкту; префікс унікальний у межах акаунта                                                                                                                                                                                                                   |
| порожнє `<body>` у prerendered HTML (SEO § 1.1)                 | `+layout.ts` вимикає SSR для всього застосунку                                                                                                                                                                                                                                                         | гра односторінкова: усі стани — стани всередині партії, а не окремі документи. Мета-теги, canonical і OG статичні в `app.html`; у sitemap рівно одна адреса. `check:build` перевіряє не «200 символів тексту», а наявність бутстрапа — тобто що сторінка бодай ЗАПУСКАЄ застосунок |
| ~~`rooms` відкриті на запис без перевірки власника (SECURITY)~~ | **закрито 2026-08-18.** Було `allow read, write: if true` — **без авторизації**: будь-хто читав кожну партію, перезаписував її стан і видаляв будь-яку кімнату. Тепер читає лише авторизований, править господар або учасник, зносить лише господар; ходи, голоси, присутність і чат звужені до автора | причина дірки лежала не в правилі, а в моделі даних: вся партія була одним документом, який писали обидва, тож власника не існувало. Модель замінено на журнал ходів (`src/lib/sync/`)                                                                                             |
| `SEC-CSP-SPREAD-HANDLER` / `GATE-INLINE-HANDLERS` (SECURITY § 6.3.2, HIGH) | гейта над `build/` немає — правило тут незастосовне | клас існує лише в SSR: інлайновий `onload="this.__e=event"` вставляє КОМПІЛЯТОР на елемент із розгортанням атрибутів. `+layout.ts` вимикає SSR цілком, тож `build/` містить оболонку SPA і `404.html`, у яких розмітки компонентів немає за побудовою. Перевірка над цими двома файлами була б рівно порожньою (AI-AGENT-PITFALLS § 1), а канон сам застерігає: нуль знайдених `.html` означає «перевірка дивиться не туди». Заміряно 2026-09-02: усі розгортання атрибутів у проєкті стоять на `<svg>` і `<button>`, жодного на `img`/`iframe`/`video` — тобто й ознаки ризику немає. **Тригер перегляду більше не абзац, а червоний тест** (`src/csp-hash.test.ts`): він падає з інструкцією, щойно якийсь маршрут поверне `ssr = true`. Саме `ssr`, а не `prerender`: `src/routes/+page.ts` має `prerender = true` увесь час, і з вимкненим SSR це дає той самий порожній каркас SPA — попереднє формулювання «увімкнення SSR або prerender» було наполовину вже виконаним. Причина ж класу (розгортання атрибутів на `img`/`iframe`/`video`/`audio`) видима в джерелах і від профілю не залежить — вона теж під гейтом там само | 2026-09-02 |
| `SKD-REGISTRY` і сім правил під ним + `GATE-DATA-REGISTRY` (SVELTEKIT-DATA § 7, HIGH) | реєстрів даних у репозиторії немає — правило незастосовне | найбільша знахідка ревізії 8.12 цілить у static-профіль із контентом у `src/lib/data/`. Тут такого каталогу немає зовсім: жодного `*.data.json` чи `*.index.json`, дані гри обчислюються, а тексти живуть у словниках i18n. **Тригер не в голові, а в гейті:** `scripts/check-build.mjs` шукає такі файли в `src/` і падає з інструкцією, щойно перший з'явиться | 2026-09-02 |
| ~~`CDB-GATE-SERVER-TIME` (CLOUD-DATABASE § 3.3.1, HIGH)~~ | **знято 2026-09-10: відхилення більше немає.** Запис був зроблений 2026-09-02 зі словами «у `firestore.rules` немає жодної звірки з `request.time`» — і того самого дня наступний коміт додав дві: `rooms/*/messages` і `feedback/*/entries` тепер вимагають `createdAt == request.time`. Гейт перевіряє їх саме так, як приписує правило — через `documents:commit` із `setToServerValue: REQUEST_TIME` (`scripts/check-rules.mjs`), бо через `createDocument` позитивні випадки виглядали б забороненими. Тобто рядок пережив свою причину на кілька годин, і жоден гейт цього не бачив: `GATE-DOC-NUMBERS` стеріг числа, а не твердження (`PIT-DOC-FACTS`) | 2026-09-10 |

### WCAG SC 2.1.4 «Character Key Shortcuts»: обраний шлях (HOTKEYS-v9)

Пʼять правил `HK-*` — зокрема два CRITICAL (`HK-TEXT-ENTRY-GUARD`,
`HK-WCAG-CHARACTER-KEY`) — перевіряються не лише гейтом, а й **записом тут**.
Доти запису не було: обраний шлях жив коментарями в
`src/lib/services/hotkeyService.ts` і `src/lib/stores/gameSettingsDefaults.ts`,
причому обидва казали «шлях 2» про різні набори клавіш.

Критерій (рівень A) вимагає для одиночного літерного скорочення **хоча б
одного** з трьох: вимкнути, перепризначити, або воно діє лише поки сфокусований
конкретний компонент.

**Глобальні одиночні літери (контекст `global`, активні на кожному екрані):**
`KeyT`, `KeyL`. Цей рядок під гейтом — див. кінець розділу.

| Набір клавіш | Де реєструється | Шлях 2.1.4 |
| --- | --- | --- |
| Ігрові: напрямки (`Q W E A D Z X C S`), `Numpad*`, `KeyM`, `KeyI`, `Escape` | контекст `game`, з `keybindings` у налаштуваннях | **перепризначення.** Сторінка `/controls` дає змінити будь-яку; порожній перелік = дію вимкнено |
| `KeyT` (тема) і `KeyL` (мова) | контекст `global`, **зашиті** в `+layout.svelte` | **шляху немає — відкритий борг, див. нижче** |

**Що вже зроблено й чого цього не досить.** `hotkeyService` виходить, поки фокус
у полі вводу (`isTypingTarget`, `closest`, а не `tagName`), і не реагує на
поєднання з модифікаторами (`isPlainKey`). Це знімає найчастіший прояв — набір
тексту виконує команди, — і саме його вимагає `HK-TEXT-ENTRY-GUARD`. Але сам
критерій 2.1.4 воно не виконує: «діє лише у фокусі» означає фокус на **тому
самому компоненті**, а не «не в полі вводу». Голосовий ввід поза полем так само
натисне `T`.

**Борг: `T` і `L` не можна ні вимкнути, ні перепризначити.** Обидві зареєстровані
беззастережно в `+layout.svelte`. У переліку дій налаштувань `toggle-theme` і
`toggle-language` є, але з порожнім типовим значенням і в контексті `game` —
тобто призначена там клавіша не знімає зашиту глобальну. Два кандидати на
закриття, обидва — робота під очима, а не вночі:

1. **Перепризначення (шлях 2, як в ігрових).** Реєструвати `global`-клавіші з
   того самого реєстру `keybindings`, а не з тіла `+layout.svelte`. Тонкощі, які
   й лишили це незробленим: типове значення `['KeyT']` дасть подвійну реєстрацію
   з контекстом `game`, і `showKeyConflictModal` питатиме про конфлікт, якого
   немає.
2. **Вимкнення (шлях 1).** Прапорець «однолітерні скорочення» в налаштуваннях і
   один вихід у `hotkeyService`. Дешевше за перший, але це новий рядок UI й
   чотири переклади.

**Під гейтом.** `src/hotkeys-conventions.spec.ts` звіряє цю таблицю з кодом:
кожна одиночна літера, зареєстрована в контексті `global`, мусить бути названа
тут. Нова глобальна літера без рядка валить прогін, а рядок про літеру, якої в
коді вже немає, валить його з другого боку.

### Мова з адреси — `?lang=`: вибір за правилом (I18N-LANG-IN-PATH, канон v9.1)

До v9 це було записаним відхиленням: канон приписував мовний СЕГМЕНТ і
забороняв параметр цілком. З 9.0 сегмент вимагається лише там, де переклади
індексуються, а з 9.1 сказано прямо: коли мова — лише налаштування і сегмента
немає, вхідний `?lang=` є вибором користувача і записується у сховище. Тут саме
так: гра односторінкова, у sitemap рівно одна адреса (рядок вище), мова живе в
налаштуваннях, тож втрачати в індексі нема чого, а параметр — єдина ручка в
адресі, яка тут існує.

Навіщо вона. Вісім сайтів автора посилаються один на одного, і доти кожне таке
посилання було голою адресою: читач, що переглядав сусідній сайт нідерландською,
приходив сюди українською. Таблиця сусідів — `src/lib/siblings.ts`, копія одного
файлу у всіх восьми репозиторіях; сусіди будують посилання сюди з рядка
`mindstep`. Приймач — `applyLanguageFromUrl()` в `i18n/init.svelte.ts`.

**Вхідна мова тут ЗАПИСУЄТЬСЯ, і це відрізняється від сайтів із мовою в шляху.**
Там поточна мова й збережений вибір — два різні місця, тож вхідна мова діє на
візит і сховища не чіпає. Тут місце одне: `appSettingsState.state.language` — це
і показ, і сховище, і прапор у шапці через `derivedState`. Застосувати мову, не
записавши її, означало б англійський інтерфейс з українським прапором на кнопці
мов.

Дрейф стереже `src/lib/siblings.test.ts`: рядок `mindstep` звіряється з
переліком `register()`, зі схемою налаштувань, з базою у `vite.config.ts`, з
origin у генераторі sitemap і з відсутністю мовного маршруту.

### Відомі вразливості залежностей, які лишаються відкритими

Гейт `npm audit --audit-level=high` у CI зелений. **Точного числа порад у прозі
тут більше немає, і це навмисно** (`PIT-NUMBER-UNDER-GATE`): попереднє —
«10 (3 low, 7 moderate), заміряно 2026-08-16» — розійшлося з дійсністю мовчки,
бо реєстр публікує нові поради без жодної зміни в цьому репозиторії. Під гейтом
стоїть не число, а рівень: усе, що доростає до `high`, валить прогін.

**Заміряно 2026-09-10: гейт БУВ ЧЕРВОНИЙ.** `browserslist@4.25.3` отримав дві
поради рівня high (необмежене зростання пам'яті й запис у прототип через
власний файл статистики browserslist-stats.json) — тобто крок `Audit dependencies` валив і `ci.yml`, і
деплой у продакшн, і причина була не в коді. Пакет транзитивний і суто збірковий
(`vite-plugin-pwa → workbox-build → @babel/*`), у бандл не потрапляє. Закрито
`npm audit fix` без `--force`: у `package-lock.json` він піднявся до `4.28.9`,
жодного мажора не зачепивши. Прогін після цього: `check`, `lint`, 486 тестів,
збірка й `check:build` зелені, критичний шлях 106.9 КБ проти бюджету 118.

Прод-залежності окремо: `npm audit --omit=dev --audit-level=high` — зелений і
до цієї правки. Нижчі рівні не зникають із поля зору (DEPENDENCIES § 4.1).

| Пакет | Рівень | Чому не закрито |
| --- | --- | --- |
| `esbuild` ← `svelte-i18n` | moderate | «фікс» — відкат `svelte-i18n` на 3.7.1, тобто кілька мажорів назад. Вразливість стосується dev-сервера, не збірки |
| `uuid`, `csv-parse`, `stream-json`, `qs` ← `firebase-tools` | moderate | лише devDependency: CLI емуляторів, у браузер відвідувача не потрапляє |
| `@opentelemetry/core` ← `@google-cloud/pubsub` ← `firebase-tools` | moderate | те саме: тільки CLI-інструмент |
| `@vitest/mocker` ← `vitest` | moderate | «фікс» — мажор `vitest@4`, тобто переписування конфігу заради читання файлів у власному прогоні |
| `cookie` ← `@sveltejs/kit` | low | static-профіль, серверного рантайму немає |

## Проєктні конвенції поза пакетом

- Словники i18n — не один файл на мову, а каталог `src/lib/i18n/<lang>/<feature>.ts`
  плюс агрегатор `<lang>.ts`. Новий текст іде у файл фічі, а не в спільний.
- Логіка режимів гри живе в `src/lib/game-modes/` окремими класами
  (`OnlineGameMode`, локальний, тренування) — не в компонентах.
- `logService.warn` для очікуваних мережевих збоїв, `error` — лише для того, що
  справді ламає застосунок (ERROR-HANDLING § 1.4).

---

## Рішення щодо AI-пошуку (SEO-v8 § 7)

| Питання                                                                            | Рішення                                   | Чому                                                                                                                                                                                                       |
| ---------------------------------------------------------------------------------- | ----------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Пошукові AI-агенти (`OAI-SearchBot`, `ChatGPT-User`, `PerplexityBot`, `ClaudeBot`) | **Дозволені**                             | Від них залежить, чи згадають сайт у відповідях ChatGPT Search, Perplexity й Claude. Службові маршрути закриті в КОЖНІЙ групі окремо                                                                       |
| Тренувальні краулери (`GPTBot`, `Google-Extended`, `CCBot`, `Bytespider`)          | **`GPTBot` дозволений, решта не названа** | Публічний хобі-проєкт без комерційного вмісту: втрати від навчання немає, а зайві заборони — це рядки, які треба супроводжувати. `GPTBot` НЕ впливає на ChatGPT Search — за неї відповідає `OAI-SearchBot` |
| `llms.txt`                                                                         | **Є**                                     | Стандарт — ставка, а не заміряний механізм: жоден великий постачальник публічно не підтвердив, що читає його. Файл коштує 30 рядків, тож ставка виправдана                                                 |

**`robots.txt` тут не читається краулерами, і це прийнято.** Сайт живе за
адресою `https://alik532ua.github.io/MindStep/`, тобто це project-сайт GitHub Pages. `robots.txt` діє лише в
корені origin (`https://alik532ua.github.io/robots.txt`), а це файл сусіднього
репозиторію. Наш лягає під префіксом проєкту, і краулер його не запитує.

Варіанти були: власний домен, зведення правил у репозиторій user-сайту, або
прийняти інертність. Обрано третє — керування індексацією тут повністю несуть
мета-теги `<meta name="robots">`, які є частиною сторінки й від origin не
залежать. `robots.txt` лишається правильним і повним, щоб переїзд на власний
домен не потребував нічого, крім `CNAME`.

Відповідність тримає `npm run check:build` (SEO-v8 § 7.5): він розбирає
`robots.txt` по групах і падає, якщо іменована група не повторює `Disallow`
з `*`, або якщо в `llms.txt` одна адреса стоїть під різними назвами.
